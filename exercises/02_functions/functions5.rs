// TODO: Fix the function body without changing the signature.
fn square(num: i32) -> i32 {
    num * num
}

fn main() {
    let number = 3;
    let answer = square(number);
    println!("The square of {number} is {answer}");
}
